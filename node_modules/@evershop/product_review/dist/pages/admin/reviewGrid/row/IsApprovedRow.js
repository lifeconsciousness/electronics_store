import Dot from '@components/common/Dot';
import PropTypes from 'prop-types';
import React from 'react';
export default function IsApprovedRow({ approved = false }) {
    return (React.createElement("td", null,
        React.createElement("div", { className: "flex justify-center" },
            approved === false && React.createElement(Dot, { variant: "default", size: "1.2rem" }),
            approved === true && React.createElement(Dot, { variant: "success", size: "1.2rem" }))));
}
IsApprovedRow.propTypes = {
    approved: PropTypes.bool.isRequired
};
//# sourceMappingURL=IsApprovedRow.js.map